export default function AddProduct() {
    return (
        <ul>
            <li>Add UI elements for adding new product by supplying fields product name, price.</li>
            <li>Add buttons to save and cancel the operation.</li>
        </ul>
    );
}
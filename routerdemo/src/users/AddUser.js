export default function AddUser() {
    return (
        <ul>
            <li>Add UI elements for adding new user by supplying fields username, email, phone, password.</li>
            <li>Add buttons to save and cancel the operation.</li>
        </ul>
    );
}